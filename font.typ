// 这个文件专门用来存放所有字体的分类列表。

// --- 新增：可变字体列表 ---
// 这里存放所有名字里带有 "Variable" 的字体。
#let variable_fonts = (
  "Noto Sans SC",
  
)

// --- 新增：我喜欢的可变字体列表 ---
/*
// #let my_favorite_variable_fonts = (
  "Noto Sans SC",
)
*/

// 我喜欢的字体
#let my_favorite_fonts = (
  "Noto Sans SC",
)

// 衬线体 (Serif): 传统、典雅，适合正文阅读。
#let serif_fonts = (
  "Noto Sans SC",
)
// 无衬线体 (Sans-serif): 简洁、现代，适合标题和界面。
#let sans_serif_fonts = (
  "Noto Sans SC",
)
// 粗衬线体 (Slab Serif): 复古、有力，适合海报和强调。
#let slab_serif_fonts = (
  "Noto Sans SC",
)

// 展示/装饰体 (Display/Decorative): 风格独特，用于吸引眼球。
#let display_decorative_fonts = (
  "Noto Sans SC",
)

// 手写/脚本体 (Script/Handwriting): 模仿手写或书法。
#let script_handwriting_fonts = (
  "Noto Sans SC",
)
// 等宽字体 (Monospaced): 每个字符宽度相同，用于编程。
#let monospaced_fonts = (
  "Noto Sans SC",
)
// 特殊用途字体 (Special-Purpose):
#let special_purpose_fonts = (
  "Noto Sans SC",
)